# Database (required)
DATABASE_URL=postgresql://user:password@localhost:5432/cc0strategy

# Multi-Chain RPC URLs
# Base Mainnet (required for live protocol)
BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_KEY

# Ethereum Mainnet (optional until contracts deployed)
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY

# API Server
PORT=3000

# Note: Contract addresses are now in src/config.ts
# No need to set them as environment variables
